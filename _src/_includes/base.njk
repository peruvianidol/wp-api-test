<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if posts.title %}{{ posts.title }}{% else %}{{ site.title }}{% endif %}</title>
</head>
<body>
  {{ content | safe }}
</body>
</html>